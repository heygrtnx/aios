# Database
DATABASE_URL="postgresql://user:password@localhost:5432/aios"

# AI (required for /v1/expose/prompt)
AI_GATEWAY_API_KEY="your-gateway-api-key"
# Optional; default: anthropic/claude-sonnet-4.5
AI_MODEL="anthropic/claude-sonnet-4.5"

# Server
PORT=3000
# Set to "production" to enable open-access limit (3 prompts/day per IP when API_KEY is not set)
# ENVIRONMENT=production

# Optional; if set, all routes require x-api-key header matching this value
# API_KEY="your-secret-api-key"

# Swagger / docs (optional)
PLATFORM_NAME="AIOS"
PLATFORM_URL="http://localhost:3000"
DEVELOPMENT_URL="localhost:3000"
PRODUCTION_URL="your-production-host.com"
